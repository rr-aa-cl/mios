apiVersion: v1
kind: Namespace
metadata:
  name: mios
  labels:
    # Host networking/IPC, hostPath, and RT capabilities require this policy.
    # The containers themselves do not run with privileged: true.
    pod-security.kubernetes.io/enforce: privileged
